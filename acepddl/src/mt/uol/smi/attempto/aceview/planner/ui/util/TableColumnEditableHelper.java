package mt.uol.smi.attempto.aceview.planner.ui.util;

public class TableColumnEditableHelper {

	// private static final int BOOLEAN_WIDTH = 25;
	// private static final int COUNT_WIDTH = 50;

	private TableColumnEditableHelper() {
	}

	// public static void configureColumns(JTable table, TableColumnEditable[]
	// columns) {
	// int index = -1;
	// for (TableColumnEditable column : columns) {
	// index++;
	// Class<?> dataClass = table.getModel().getColumnClass(index);
	// if (dataClass.equals(Integer.class)) {
	// // table.getColumnExt(column.getName()).setPreferredWidth(COUNT_WIDTH);
	// table.getColumnModel().getColumn(index).setMaxWidth(COUNT_WIDTH);
	// } else if (dataClass.equals(Boolean.class)) {
	// // table.getColumnExt(column.getName()).setPreferredWidth(BOOLEAN_WIDTH);
	// table.getColumnModel().getColumn(index).setMaxWidth(BOOLEAN_WIDTH);
	// }
	// }
	// }
}